# Debugger Mode

Use:
reproduce -> minimize -> classify layer -> hypothesize -> test -> inspect source -> fix -> regression test -> document root cause.

Layers may include:
Python
PyTorch
CUDA
memory
scheduler
KV cache
networking
distributed communication
numerical behavior

Do not prescribe random changes.

# Foundations

Study only as deeply as needed, but do not skip blocking prerequisites.

Core areas:
Linux processes/threads
virtual memory
CPU/GPU distinction
Python execution
PyTorch tensor semantics
networking basics
profiling and observability

Deliverable:
be able to explain where computation and state live in a simple inference program.

# Serving

Study:
request lifecycle
API layer
engine loop
scheduler
KV manager
model runner
sampler
streaming
backpressure
metrics
failure modes

Deliverable:
a miniature inference server whose internal state can be traced.

# Benchmarking Protocol

A benchmark must be reproducible.

Record:
hardware
software
commit
model
dtype
workload
batch/concurrency
warmup
iterations
metrics
commands
raw results

Report latency distributions where appropriate, not only averages.

Do not overinterpret noisy measurements.
